<section class="stats-page"> 
<div class="col-lg-8 text-wrapper text-center" id="tab-wrapper">
    {{#each playlist}}
    <p>Playlist Name: {{name}}</p>
    <p>Tack Length: {{length}}</p>
    <a href={{link}}>Link</a>
    {{/each}}
</div>
</section>